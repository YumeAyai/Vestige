# jianji CloudBase function

This directory is the upload root for `tcb fn deploy`.

Build the Linux Go executable before deploying:

```sh
sh scripts/build-tcb-function.sh
```

The generated `main` binary is ignored by git.
